[user]
    name = tom-shimoda
    email = shimoda0204@gmail.com
[core]
    autocrlf = input
    pager = delta
[interactive]
    diffFilter = delta --color-only
[delta]
    features = side-by-side line-numbers
    navigate = true  # use n and N to move between diff sections

